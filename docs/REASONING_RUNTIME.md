# Reasoning runtime
Versioned stacks are snapshotted per workflow; three required role adapters feed the existing evidence council. PAPER_LIVE uses a deterministic local PaperOrderWriter and PaperAccount; receipts are simulated and top-of-book-only. No Binance API is called. Readiness is local bounded execution only.
