# 0-infinity Execution Skill

Carry approved work into reality without inventing architecture.

Sequence:

```text
verify base + canonical docs + Change Record
→ implement minimal delta
→ build/typecheck
→ unit tests
→ negative tests
→ integration tests
→ evidence capture
→ handoff to Review
```

Never bypass Council/mandate evaluation, add exchange write authority outside OrderWriter, mutate Ground Truth, or weaken refusal paths.
