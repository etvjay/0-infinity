# 0-infinity Ground Truth Skill

Promote a claim only when implementation + tests + negative tests + review + required runtime evidence exist.

Never promote target architecture because documentation exists; never call shadow live; never call an ACK profitable; never convert assumption into fact.
