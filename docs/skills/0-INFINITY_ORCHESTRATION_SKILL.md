# 0-infinity Orchestration Skill

Mission: many coding agents, one Ground Truth, one interface set, one reviewed integration branch.

Process: decompose by dependencies/write surfaces; freeze interfaces; create one Change Record per task; assign bounded branches; run tests immediately; integrate reviewed patches; run negative/race/crash tests; independent review; promote Ground Truth only with evidence.

Every Binance task must read `BINANCE_SOURCE_TRUTH.md` and verify consequential API behavior against current official Binance docs.
